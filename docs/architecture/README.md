# Architecture

The system uses a Monorepo structure with `client` (React) and `server` (Node.js/Express) workspaces.
- **Frontend**: React without JSX, Webpack 5, Tailwind CSS.
- **Backend**: Express.js, MongoDB (Mongoose), Redis, Socket.IO.
- **Testing**: Vitest, Supertest.
