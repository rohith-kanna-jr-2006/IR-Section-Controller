# API Documentation

All API routes are prefixed with `/api/v1`.
Current endpoints:
- `GET /api/v1/health` - Check application health.
- `GET /api/v1/ready` - Check application readiness (DB and Redis connectivity).
